return {
  "nvim-mini/mini.surround",
  opts = {
    mappings = {
      add = "<leader>f", -- Add surrounding in Normal and Visual modes
      delete = "<leader>fd", -- Delete surrounding
      find = "<leader>ff", -- Find surrounding
      find_left = "<leader>fF", -- Find surrounding left
      highlight = "<leader>fh", -- Highlight surrounding
      replace = "<leader>fr", -- Replace surrounding
      update_n_lines = "<leader>fn", -- Update n_lines
    },
  },
}
